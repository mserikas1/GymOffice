@page "/"
@inject NavigationManager NavigationManager

<PageTitle>GymOffice</PageTitle>

<AuthorizeView Roles="Admin">
    @{
        NavigationManager.NavigateTo("/admin", true);
    }
</AuthorizeView>
<AuthorizeView Roles="Receptionist">
    @{
        NavigationManager.NavigateTo("/receptionist", true);
    }
</AuthorizeView>
<AuthorizeView Roles="Coach">
    @{
        NavigationManager.NavigateTo("/coach", true);
    }
</AuthorizeView>